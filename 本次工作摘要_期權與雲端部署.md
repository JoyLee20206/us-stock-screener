# 工作摘要：雲端部署 + 期權工具完整建置

**日期範圍**：2026-05-31 ~ 2026-06-01
**起點**：純本機 Streamlit 美股選股 App
**終點**：雲端自動化 + 完整期權買方工具系統

---

## 🎯 完成的三大里程碑

### 里程碑 A：上雲端（GitHub Actions + Streamlit Community Cloud）
從本機跑 → 全自動雲端服務，**零成本**。

### 里程碑 B：擴充標的池
從 524 檔（指數成分股）→ ~1,000 檔（含 S&P MidCap 400 + 80 檔 ETF）。

### 里程碑 C：期權買方完整工具系統
從零到完成 Phase 0 + P1 + P2 三階段，含教學、瀏覽、分析、模擬、回測、警示等。

---

## 📅 主要工作節點

### Day 1（2026-05-31）

#### 1. 上雲方案規劃與選擇
- 評估 3 套方案：A) Railway $5/月、B) GitHub+Streamlit 免費、C) Oracle Cloud VM 免費
- 選定 **B（GitHub + Streamlit Cloud）**

#### 2. 程式碼整備
- 建立 `README.md`、`.gitignore`、`.streamlit/secrets.toml`
- 改寫 `us_screener_ui.py` 的 `load_stock_data()` 支援雲端 PARQUET_URL
- 檔案改名：`fetch_us_cache.py` → `fetch_cache_us.py`；`us_screener1.py` → `us_screener_ui.py`

#### 3. GitHub 設置
- 創建 repo `JoyLee20206/us-stock-screener`
- 拖拉上傳所有檔案
- 建立 `.github/workflows/fetch.yml`（每日股價抓取排程）

#### 4. 標的池擴充
- 加入 **S&P MidCap 400**（Minervini 風格甜蜜點）
- 從 524 檔 → 920 檔

#### 5. Streamlit Cloud 部署成功
- 設定 PARQUET_URL secret
- App 正式上線

#### 6. UI 優化（多輪迭代）
- 標題字體縮小（2.5rem → 1.6rem）
- 解決標題被 Streamlit toolbar 遮住問題（padding-top: 4rem）
- 大盤狀態 metric 字體縮小（2.25rem → 1.4rem）
- 紅綠燈訊息橫條縮緊（padding 0.4rem）
- 執行量化掃描按鈕從主畫面移到側邊欄
- 加 `now_tpe()` 統一台北時區（雲端跑 UTC 會差 8 小時）
- 解析 HTTP `Last-Modified` 顯示真實檔案更新時間

#### 7. 期權新手指南建立
- 寫 `期權新手指南.md`（10 章，280 行）
- 涵蓋 Call/Put、ITM/ATM/OTM、Greeks、IV、新手 7 大死亡陷阱

#### 8. 期權工具 Phase 0：基礎建構
建立 `options_data.py`：
- Black-Scholes Greeks（Δ/Γ/Θ/Vega，math.erf 不依賴 scipy）
- 期權鏈抓取（含 `enrich_chain` 加 Greeks + BE + 距現價%）
- 智能標籤 9 種：⭐💎🟠⏱️🟡⚠️🔥💀❓
- `build_buyer_view()` 高層 API：抓鏈 + 標籤 + 推薦合約一條龍

UI 加入「📚 期權新手教學」+「🎯 期權瀏覽」兩個 expander，含⭐ 推薦合約卡。

#### 9. 期權工具 P1：避開新手陷阱
- **進場前 7 項風險清單**（紅黃綠燈 + GO/CAUTION/STOP 判定）
- **What-if 模擬器**（3 軸 slider：股價 / 天數 / IV）
- **到期日損益曲線**（Plotly + 自動標 BE/K/現價/最大虧損）
- **教學提示**（help tooltips + 名詞解釋 expander）

#### 10. 期權工具 P2：嚴肅交易功能
- **期權持倉模組**：擴充 positions.json 支援 `type: "option"`，含 yfinance 即時鏈損益、Greeks、自動警示
- **對 Top N 產生期權建議**：掃描完批次查詢推薦合約
- **IV Rank 累積框架**：新增 `fetch_iv_history.py` + `.github/workflows/fetch_iv.yml`

#### 11. IV/RV Rank 雙軌設計
解決 yfinance 不給歷史 IV 的問題：
- **A 軌**：每日抓 ATM IV 累積到 `iv_history.parquet`（30 天後可用真實 IV Rank）
- **B 軌**：用既有 1 年股價算「20 日 Realized Volatility Rank」，立即可用
- UI 自動切換：累積 ≥ 30 天用真實 IV Rank，否則用 RV Rank（標「估算」）

---

### Day 2（2026-06-01）

#### 12. 財報日警示
新增 `get_next_earnings()`（從 yfinance calendar / earnings_dates 抓取）：
- 跨財報合約標 🚨
- 風險清單第 7 項
- 摘要列第 6 欄顯示下次財報日
- 跨財報時頂端紅色橫條
- 期權持倉自動警示「🚨 財報倒數 N 天」

#### 13. ETF 池補強
解決 XLK / VEA / VTI 在持倉找不到的問題：
- `fetch_cache_us.py` 新增 `get_etf_tickers()` 函式
- 80 檔 ETF：大盤 / 板塊 / 主題 / 國際 / 債券 / 商品 / 槓桿 / 加密
- 總標的 920 → ~1,000+ 檔

#### 14. 雲端快取優先級 Bug 修復
**關鍵 bug**：repo 內舊的 `cache/us_daily.parquet`（524 檔）會被 `load_stock_data()` 優先讀取，蓋過 Releases 上的新版（1,012 檔）。
- 修改邏輯為「有 PARQUET_URL 一律先用 URL」
- 加「🔄 刷新雲端資料」按鈕（`st.cache_data.clear()`）

#### 15. UI 大改造：4 個 expander → 頁籤（Excel 風格）
4 個底部區塊：
- 📌 我的持倉
- 📈 績效回測
- 📚 期權新手教學
- 🎯 期權瀏覽（純買方視角）

整合成 `st.tabs(...)`，CSS 同步加 `[data-baseweb="tab-panel"]` 選擇器讓標題字體一致縮小。

#### 16. session_state 持久化
解決 Streamlit 的「按按鈕載入結果，互動其他元件時結果消失」問題：
- 期權瀏覽查詢結果存到 `st.session_state["opt_view"]`
- 批次推薦結果存到 `st.session_state["batch_recs"]`
- 改 ticker/expiration 時自動清空、提示重查

#### 17. Bug 系統檢查（4 個發現並修復）
- 「下方」期權瀏覽提示語過時（改頁籤導引）
- 批次查詢結果消失（同前述 session_state 修法）
- `missing_sids` 重複代號（用 `sorted(set(...))` 去重）
- 各種「下方」字眼確認都在合理脈絡

#### 18. UX 文字加強
- **期權瀏覽使用說明**：操作流程、常見情境 A/B/C/D、新手 5 大警示
- **智能標籤一覽表**：可考慮 5 種 + 應避開 5 種 + 判定流程
- **到期日損益曲線圖解**：三條虛線含義、X/Y 軸、核心三段、3 個常見 Q&A（動態帶入合約數字）
- **名詞解釋**從底部移到風險清單下方

#### 19. README 完整更新
重寫 README.md（~280 行 → ~330 行）：
- 介面總覽（主畫面 + 4 頁籤）
- 期權工具系統完整章節（7 個子功能）
- 部署架構圖加入 IV workflow
- 常見問題加入 IV Rank、財報日、ETF 等新議題
- 風險警語強調期權買方 70-80% 歸零統計

---

## 📂 最終檔案清單

### 新增（這次對話建立）
| 檔案 | 用途 |
|------|------|
| `options_data.py` | ~700 行：BS Greeks + 智能標籤 + 風險清單 + What-if + IV/RV Rank + 財報抓取 + 批次推薦 |
| `期權新手指南.md` | ~280 行：完整新手教學 |
| `fetch_iv_history.py` | ~130 行：每日 IV snapshot 累積 |
| `.github/workflows/fetch.yml` | 股價快取排程 |
| `.github/workflows/fetch_iv.yml` | IV 累積排程 |
| `本次工作摘要_期權與雲端部署.md` | 本檔 |

### 大幅修改
| 檔案 | 改動規模 |
|------|---------|
| `us_screener_ui.py` | ~900 → ~1,700 行（期權工具 + 頁籤 + 雲端邏輯 + UI 優化）|
| `fetch_cache_us.py` | 加 SP400、ETF 池、台北時區（新增 ~130 行）|
| `README.md` | 完全重寫，加期權工具章節 |

---

## 🔑 重要決策與發現

### 1. 為什麼選 GitHub + Streamlit 而不是 Railway / Oracle VM
- 完全免費（公開 repo Actions 無限制）
- 不需信用卡驗證
- 程式改動可接受
- 缺點是持倉備份要手動 JSON

### 2. 為什麼需要 IV/RV 雙軌
- yfinance **不提供歷史 IV 回填**
- 必須每天當下抓取累積，至少 30 天才有 IV Rank
- 用既有股價算 RV Rank 當過渡期替代

### 3. 為什麼需要「雲端 URL 優先」邏輯
- 早期上傳時把 `cache/us_daily.parquet` 也推到 repo 內
- 之後 GitHub Actions 寫的是 Releases，而非 repo 內
- 原邏輯「本地有檔案就用本地」導致永遠用舊版

### 4. 為什麼要 session_state
- Streamlit 互動 = 整頁 rerun
- 沒存 session_state 的話，按完查詢按鈕後拖 slider 結果會消失
- 凡是「按一次載入大量資料、之後做互動」的場景都要套這個模式

### 5. 為什麼整合成頁籤
- 4 個 expander 並列容易很長、滾動疲勞
- Tabs 一次只看一個，視覺乾淨且像 Excel sheet

---

## 🎓 留下的待擴充項目

| 功能 | 工時估算 |
|------|---------|
| 財報前 IV 突增偵測（earnings drift） | 1-2 天 |
| 自動推薦「財報後到期」的合約 | 0.5 天 |
| 期權持倉的歷史損益曲線 | 2 天 |
| 波動率曲面（Skew）視覺化 | 2-3 天 |
| 多券商持倉自動同步 | 5+ 天（需 IB / TD API）|

---

## 📊 成果指標

| 指標 | 起點 | 終點 |
|------|------|------|
| 主程式行數 | ~900 | ~1,700 |
| 模組數 | 1 | 4 + 1 markdown |
| 標的池 | 524 | ~1,000+ |
| 部署方式 | 本機 | 雲端全自動 |
| GitHub Actions workflows | 0 | 2 |
| 期權工具 | 無 | 完整買方系統 |
| 教學文件 | 無 | 280 行新手指南 + App 內使用說明 + 名詞解釋 + 圖解 |
| 自動排程 | 無 | 每日凌晨 5:30 + 5:45 |
